# P01.3 Build and Schema Safety Log — 2026-07-11

## Completed

- Changed the application build command to run `prisma generate && next build` only.
- Added `bun run db:deploy` for the explicitly invoked `prisma migrate deploy` operation.
- Updated the deployment guidance in `AGENTS.md`, `CLAUDE.md`, and `SISTEM_DOKUMANTASYONU.md` so a build never mutates the database schema.

## Verification

- `bun test`, `bun run lint`, `bun run typecheck`, and `bun run build` exited successfully.
- The build script no longer contains `prisma db push`.
- `AGENTS.md` and `CLAUDE.md` remain identical.
- A read-only `prisma migrate diff` against the production direct connection returned an empty migration, so the current Prisma schema matches production.

## Deferred

There is no migration history baseline yet. Creating and deploying an empty baseline migration writes to the production migration metadata table, so it remains deferred until a backup, staging rehearsal, and explicit production-write approval are available.
