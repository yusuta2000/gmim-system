# P00.2 Repository Hygiene Log — 2026-07-11

## Scope

Remove production database connection material from the current tracked tree. No database, Vercel, Neon, or remote Git state was changed.

## Completed

- Removed the tracked `.env` file from the Git index while preserving the local file.
- Added `.env.example` with non-sensitive placeholders.
- Changed the legacy import and viewer-user scripts to require `DATABASE_URL` and, where applicable, `DIRECT_URL` from the environment.
- Replaced connection material in `SISTEM_DOKUMANTASYONU.md` with placeholders and a local setup instruction based on `.env.example`.
- Added an explicit `.gitignore` exception so `.env.example` can be tracked.

## Verification

- `python -m py_compile scripts/add_viewer_users.py scripts/import_excel.py scripts/import_excel_fast.py`
- `git diff --check`
- Tracked-tree scan for the known production host and database user markers: no matches.
- Confirmed that `.env` is not tracked and remains present locally.

## Deferred: P00.3 History Cleanup

The former credential existed in Git history. Do not rewrite history or force-push during normal development. Coordinate both developers first, take a backup, use a dedicated mirror clone and an approved history-filtering procedure, scan every rewritten ref, force-push only with explicit repository-owner approval, and require all collaborators to reclone or reset according to the agreed procedure. The credential rotation already limits the exposure of the old value.
